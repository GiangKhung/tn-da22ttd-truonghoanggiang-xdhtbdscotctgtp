# Đặt URL API backend Next.js ở đây.
# Khi chạy Expo Go trên điện thoại thật, KHÔNG dùng localhost — phải dùng IP LAN
# của máy đang chạy `npm run dev:lan`. Tìm IP bằng `ipconfig` trên Windows.
# Cổng mặc định Next dev là 3000.
EXPO_PUBLIC_API_BASE_URL=http://192.168.1.10:3000
